# Current Task

**Next: v5** *(not yet scoped — start of a new conversation)*

v4 is complete through v4d. v4a (selection polish + right-click menus), v4b (play-range bar + I/O hotkeys + playhead scrub), v4c (module split into `sb_shot_model.py` + `sb_persistence.py` + `sb_canvas.py` + entry-point `shotblocks.pyp`), and **v4d (canvas polish — centered-track NLE layout, visible edge bands with hover highlight, group multi-shot drag, playhead triangle handle, selection-as-fill, BaseLink camera tracking)** are all archived.

v5 candidates from the architecture (still unchanged): orphaned-shot handling (camera deletion intercept), audio subsystem (waveform + onset detection), spacebar playback engine, preset library, slate engine, bake-down. Pick one as v5 in the next session.

The user has noted some small visual-design clean-up items they want to revisit later — defer until v5+ features (audio, orphans, presets, etc.) are landed and the visual language can be tuned holistically.
